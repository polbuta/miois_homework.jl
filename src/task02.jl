function arraysum(ar::AbstractVector{<:Number})::Float64
    sum = 0;
    for i in number
        sum+=i
    end
    return sum;
end


function mul35sum(N::Integer)::Int64
    sum = Int64(0);
    return sum;
end
